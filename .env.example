# Copy to .env and fill in. .env is gitignored.
FRED_API_KEY=            # required
EIA_API_KEY=             # optional; fetch_slow skips EIA if absent
ACLED_EMAIL=             # optional; only if you build a custom conflict measure
ACLED_KEY=
